# Decision context

The canonical current decision log is [../CONTEXT.md](../CONTEXT.md).

This indirection avoids the two previously duplicated context files drifting to
different models, databases, test counts, and evaluation claims.
